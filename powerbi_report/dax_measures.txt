Total AUM = SUMX(VALUES(dim_fund[amfi_code]), CALCULATE(MAX(fact_nav[nav])) * 1000)
SIP Inflows = SUM(fact_transactions[amount_inr])
Folios = 261200000
Schemes = DISTINCTCOUNT(dim_fund[amfi_code])
AUM by AMC = SUMX(VALUES(dim_fund[fund_house]), CALCULATE(MAX(fact_nav[nav])) * 1000)
Return vs Risk Scatter = AVERAGE(fact_performance[return_1yr_pct])
Fund Score = DIVIDE([Return vs Risk Scatter] + [Sharpe Ratio], 2)
Sharpe Ratio = AVERAGE(fact_performance[sharpe_ratio])
Transaction Amount by State = SUM(fact_transactions[amount_inr])
SIP/Lumpsum/Redemption Split = COUNTROWS(fact_transactions)
Avg SIP Amount by Age = AVERAGEX(FILTER(fact_transactions, fact_transactions[transaction_type] = "SIP"), fact_transactions[amount_inr])
Monthly Transaction Volume = COUNTROWS(fact_transactions)
SIP Inflow Trend = SUM(fact_transactions[amount_inr])
Nifty 50 Trend = AVERAGE(0)
Category Inflow = SUM(fact_transactions[amount_inr])
Top 5 Categories = TOPN(5, SUMMARIZE(dim_fund, dim_fund[category], "Inflow", [Category Inflow]), [Inflow], DESC)
